name: Test
on:
  pull_request:
    branches:
      - "main"
jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: install
        run: pnpm install
      - name: start
        run: pnpm dev
      - name: testing
        run: pnpm test
